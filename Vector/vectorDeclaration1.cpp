#include<bits/stdc++.h>
using namespace std;
int main ()
{
     int arr[10];  // in array we have to declare size,array=static


     vector<int>v;
     
     /* 
      vector is a container where we can store data,
      in vector we dont declare its size bcz
      its dynamic array where size can be change always.
      we can take int,char,float as always

     */
}